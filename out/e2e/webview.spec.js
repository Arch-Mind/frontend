"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
const test_1 = require("@playwright/test");
const path = __importStar(require("path"));
test_1.test.describe('ArchMind Webview E2E', () => {
    (0, test_1.test)('should load the application root', async ({ page }) => {
        // In a real scenario, we'd point to the built index.html or a dev server
        // For now, let's assume we're testing the built bundle in a mock HTML
        const htmlPath = path.resolve(__dirname, '../out/webview/index.html');
        // If index.html doesn't exist yet (it's usually generated by the extension at runtime),
        // we can use a data URL or a temp file for this test.
        await page.goto(`file://${htmlPath}`);
        // Basic check: root element should exist
        const root = page.locator('#root');
        await (0, test_1.expect)(root).toBeVisible();
    });
    (0, test_1.test)('should show loading state', async ({ page }) => {
        const htmlPath = path.resolve(__dirname, '../out/webview/index.html');
        await page.goto(`file://${htmlPath}`);
        // The app usually starts with a loading message if no data is present
        await (0, test_1.expect)(page.locator('body')).toContainText(/analyzing/i, { timeout: 10000 });
    });
});
//# sourceMappingURL=webview.spec.js.map